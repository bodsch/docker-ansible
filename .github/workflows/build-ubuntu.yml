---
name: Ubuntu Ansible Image (build, test, publish)

on:
  push:
    branches:
      - master
      - "feature/**"
    paths:
      - "ubuntu-ansible/**"
      - "tests/**"
      - ".github/workflows/build-ubuntu.yml"
      - "!README.md"
      - "!Makefile"
  pull_request:
    branches:
      - master
      - "feature/**"
    paths:
      - "ubuntu-ansible/**"
      - "tests/**"
      - ".github/workflows/build-ubuntu.yml"
      - "!README.md"
      - "!Makefile"
  schedule:
    # GitHub cron ist UTC
    - cron: "5 19 * * 0"
  workflow_dispatch:

concurrency:
  group: ubuntu-ansible-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

env:
  # Ziel-Images
  IMAGE_DOCKERHUB: bodsch/ansible-ubuntu
  IMAGE_GHCR: ghcr.io/bodsch/docker-ansible/ansible-ubuntu

  # Lokales Test-Image
  LOCAL_IMAGE: ansible-ubuntu

  # Inspec
  INSPEC_VERSION: "5.21.29"

jobs:
  build_test:
    name: Build & Test (${{ matrix.distribution.version }})
    runs-on: ubuntu-24.04
    strategy:
      fail-fast: false
      matrix:
        distribution:
          - image: ubuntu
            version: "20.04"
          - image: ubuntu
            version: "22.04"
          - image: ubuntu
            version: "24.04"

    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Set up Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build (load for tests)
        uses: docker/build-push-action@v6
        with:
          context: ubuntu-ansible
          file: ubuntu-ansible/Dockerfile.ubuntu-${{ matrix.distribution.version }}
          platforms: linux/amd64
          load: true
          tags: ansible-ubuntu:${{ matrix.distribution.version }}
          pull: true
          # Rebuild ohne Cache nur beim Scheduled Run (typisch für Security Updates)
          no-cache: ${{ github.event_name == 'schedule' }}
          cache-from: type=gha,scope=${{ matrix.distribution.image }}-${{ matrix.distribution.version }}
          cache-to: type=gha,mode=max,scope=${{ matrix.distribution.image }}-${{ matrix.distribution.version }}

      # - name: Install InSpec
      #   run: |
      #     set -euo pipefail
      #     curl -sSL https://omnitruck.chef.io/install.sh | sudo bash -s -- -P inspec -v "${INSPEC_VERSION}"
      #     inspec --version
      #
      # - name: Start test container
      #   run: |
      #     set -euo pipefail
      #     name="ansible-ubuntu-${{ matrix.distribution.version }}"
      #     image="${{ env.LOCAL_IMAGE }}:${{ matrix.distribution.version }}"
      #
      #     docker rm -f "${name}" >/dev/null 2>&1 || true
      #
      #     docker run -d \
      #       --name "${name}" \
      #       --hostname "${name}" \
      #       --privileged \
      #       --env container=docker \
      #       --volume /sys/fs/cgroup:/sys/fs/cgroup:rw \
      #       --cgroupns=host \
      #       "${image}"
      #
      # - name: Run InSpec tests
      #   run: |
      #     set -euo pipefail
      #     name="ansible-ubuntu-${{ matrix.distribution.version }}"
      #     image="${{ env.LOCAL_IMAGE }}:${{ matrix.distribution.version }}"
      #
      #     inspec exec tests/inspec/ \
      #       --input image="${image}" container_name="${name}" \
      #       --chef-license accept-silent \
      #       --reporter cli
      #
      # - name: Cleanup
      #   if: always()
      #   run: |
      #     set -euo pipefail
      #     docker rm -f "ansible-ubuntu-${{ matrix.distribution.version }}" >/dev/null 2>&1 || true

  publish:
    name: Publish (${{ matrix.distribution.version }})
    needs: build_test
    runs-on: ubuntu-24.04
    if: |
      github.ref == 'refs/heads/master' &&
      (github.event_name == 'push' || github.event_name == 'schedule' || github.event_name == 'workflow_dispatch')
    permissions:
      contents: read
      packages: write

    strategy:
      fail-fast: false
      matrix:
        distribution:
          - image: ubuntu
            version: "20.04"
            platform: linux/amd64
          - image: ubuntu
            version: "22.04"
            platform: linux/amd64
          - image: ubuntu
            version: "24.04"
            platform: linux/amd64

    steps:
      - name: Checkout
        uses: actions/checkout@v6

      # QEMU nur nötig, wenn ihr später arm64 etc. ergänzt
      - name: Set up QEMU
        if: contains(matrix.distribution.platform, 'arm')
        uses: docker/setup-qemu-action@v3

      - name: Set up Buildx
        uses: docker/setup-buildx-action@v3

      - name: Login to GHCR (via GITHUB_TOKEN)
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: bodsch
          password: ${{ secrets.GH_DOCKER_REGISTRY_TOKEN }}

      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: Docker metadata (tags/labels)
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: |
            ${{ env.IMAGE_DOCKERHUB }}
            ${{ env.IMAGE_GHCR }}
          tags: |
            type=raw,value=${{ matrix.distribution.version }}
            type=sha,format=short,prefix=${{ matrix.distribution.version }}-

      - name: Build & push
        uses: docker/build-push-action@v6
        with:
          context: ubuntu-ansible
          file: ubuntu-ansible/Dockerfile.ubuntu-${{ matrix.distribution.version }}
          platforms: ${{ matrix.distribution.platform }}
          push: true
          pull: true
          # optional: scheduled rebuild ohne Cache
          no-cache: ${{ github.event_name == 'schedule' }}
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=gha,scope=${{ matrix.distribution.image }}-${{ matrix.distribution.version }}
          cache-to: type=gha,mode=max,scope=${{ matrix.distribution.image }}-${{ matrix.distribution.version }}
